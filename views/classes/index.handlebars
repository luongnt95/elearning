<h3> Classes </h3>

	{{#each classes}}
		<div class="col_4 block">
			<h4> {{title}} </h4>
			<p>{{{description}}} </p>
			<p> Instructor: <strong> {{instructor}} </strong></p>
			<a class="button" href="/classes/{{_id}}"> Class Details </a>
		</div>
	{{/each}}
